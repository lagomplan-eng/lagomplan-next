# dependencies
node_modules

# next.js
.next
out

# env files
.env.local
.env

# mac
.DS_Store

node_modules
.next
.env
.env.local
.vercel

tsconfig.tsbuildinfo

# Smart Finds prototype source material (raw image dumps + image
# normalizer backups). Originals stay on disk for reference but
# don't belong in the repo.
prototypes/
public/images/smart-finds/_backup/

# Playwright / test artifacts
/playwright-report/
/test-results/
/blob-report/
/playwright/.cache/

# Supabase CLI temp
